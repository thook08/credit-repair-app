import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { ThemedText } from '../components/ThemedText';
import { ThemedView } from '../components/ThemedView';

export default function CreditDashboard() {
  return (
    <ThemedView style={styles.container}>
      <ThemedText style={styles.title}>Your Credit Dashboard</ThemedText>
      <View style={styles.box}>
        <Text style={styles.scoreLabel}>Credit Score:</Text>
        <Text style={styles.scoreValue}>720</Text>
      </View>
      <View style={styles.box}>
        <Text>Disputes in progress: 2</Text>
        <Text>Last updated: Today</Text>
      </View>
    </ThemedView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
  },
  title: {
    fontSize: 22,
    marginBottom: 30,
    fontWeight: 'bold',
  },
  box: {
    marginBottom: 20,
    padding: 15,
    backgroundColor: '#eee',
    borderRadius: 10,
  },
  scoreLabel: {
    fontSize: 16,
  },
  scoreValue: {
    fontSize: 36,
    fontWeight: 'bold',
    color: 'green',
  },
});
